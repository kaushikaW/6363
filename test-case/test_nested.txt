// Test 8: Nested Function Calls and Object Access
class Calculator {
    public func add(a : integer, b : integer) => integer {
        return (a + b);
    }
    
    public func multiply(a : integer, b : integer) => integer {
        return (a * b);
    }
};

func testNested() => void {
    local calc : Calculator;
    local result : integer;
    
    result := calc.add(5, calc.multiply(2, 3));
    write(calc.add(result, 10));
    
    if (calc.add(1, 2) > calc.multiply(1, 1)) then {
        calc.add(calc.multiply(2, 3), calc.add(4, 5));
    } else {
    };
}