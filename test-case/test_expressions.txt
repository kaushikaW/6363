// Test 5: Complex Expressions and Statements
func complexFunction() => integer {
    local a : integer[5];
    local b : float[];
    local result : integer;
    
    a[0] := 10 + 20 * 3;
    b[1] := 3.14 / 2.0 - 1.5;
    result := (a[0] + 5) * (10 - 3);
    
    if (result > 100) then {
        write(result);
    } else {
        result := 0;
    };
    
    while (result < 50) {
        result := result + 1;
    };
    
    return (result);
}